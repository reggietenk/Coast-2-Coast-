import React from 'react';

// import Auth from '../utils/auth';

const Home = () => {

	// const loggedIn = Auth.loggedIn();

  return (
    <main>
			<h1>Hello World</h1>
    </main>
  );
};

export default Home;
